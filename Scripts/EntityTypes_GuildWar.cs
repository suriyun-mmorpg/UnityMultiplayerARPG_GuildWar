using MultiplayerARPG.MMO.GuildWar;
using System;

namespace MultiplayerARPG
{
    public static partial class EntityTypes
    {
        public static readonly string GuildWarMonster = typeof(GuildWarMonsterCharacterEntity).ToString();
    }
}
